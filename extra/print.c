#include <stdio.h>
void main() {
int a=10;
float b=12.5;
char c='z';

   // printf() displays the string inside quotation
   printf("the value of a is %d also b is %f and c is %c \n", a,b,c);

   printf("the value of a is %d \n",a);
      printf("the value of b is %f \n ",b);
   printf("the value of c is %c \n", c);

}